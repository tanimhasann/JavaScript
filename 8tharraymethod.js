let number=['Tanim','rafi','skaib'];

number.push('lion')//add the new value in the last last element
console.log(number);
number.shift()//its remove the first element of the array
console.log(number);
number.unshift('hasib');//its add the new value at 1st position
console.log(number);
number.pop();//pop delete the last element of the array
console.log(number);